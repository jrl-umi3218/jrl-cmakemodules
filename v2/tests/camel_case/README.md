# camel_case

Check that CamelCase projects and targets names are supported by jrl-cmakemodules.
When exporting files, <project-name>-config.cmake does not support camel case but <ProjectName>Config.cmake supports CamelCase.
