# comp_genexp

Multi-component export where target properties use **generator expressions**.
This is testing the support for generator expressions with jrl-v2, which requires some special handling.
Generator expressions are only evaluated during the "genration phase" in CMake.
