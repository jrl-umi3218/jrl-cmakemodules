# exported_headers

Tests that `jrl_target_headers()` correctly installs a public header from an `INTERFACE` library, verified by a consumer building against the exported target.
Also checks that private headers are not installed/exported.
