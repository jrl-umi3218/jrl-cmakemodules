# find_package_build_interface

Same dependency matrix as `link_with_build_interface`, but the dependencies come from
`jrl_find_package()` calls: the generated `dependencies.cmake` must be identical.
