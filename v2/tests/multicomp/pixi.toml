[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-arm64", "osx-64", "win-64"]
preview = ["pixi-build"]

[dependencies]
multicomp-consumer = { path = "multicomp-consumer" }

[tasks]
test = { cmd = "echo Test completed successfully!" }
