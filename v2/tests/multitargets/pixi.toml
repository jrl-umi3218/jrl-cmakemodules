[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-arm64", "osx-64", "win-64"]
preview = ["pixi-build"]

[dependencies]
multitargets-consumer = { path = "multitargets-consumer" }

[tasks]
test = { cmd = "echo Test completed successfully!" }
