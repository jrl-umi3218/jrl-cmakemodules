# Target Headers Relative

Verifies that `jrl_target_headers()` works with relative paths when targets and headers are defined in a subdirectory, and `jrl_export_package()` is called from the main directory.
