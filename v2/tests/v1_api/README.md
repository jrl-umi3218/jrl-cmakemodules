# v1 API

Here we verify that the v1 API still works as expected, even though we are shipping and integrating the v2 API.
This test is important to make sure we don't break the v1 API users while they are migrating to v2.
